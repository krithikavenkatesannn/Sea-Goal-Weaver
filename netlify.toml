[build]
  command = "corepack enable && corepack prepare pnpm@latest --activate && pnpm install && pnpm --filter @workspace/ocean-goals run build:netlify"
  publish = "artifacts/ocean-goals/dist/public"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
