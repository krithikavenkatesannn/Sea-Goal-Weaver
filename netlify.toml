[build]
  command = "pnpm install && pnpm --filter @workspace/ocean-goals run build:netlify"
  publish = "artifacts/ocean-goals/dist/public"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
