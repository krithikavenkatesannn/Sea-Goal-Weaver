kind = "web"
previewPath = "/"
title = "Ocean Goals"
version = "1.0.0"
id = "artifacts/ocean-goals"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23582

[services.development]
run = "pnpm --filter @workspace/ocean-goals run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ocean-goals", "run", "build" ]
serve = "static"
publicDir = "artifacts/ocean-goals/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23582"
BASE_PATH = "/"
